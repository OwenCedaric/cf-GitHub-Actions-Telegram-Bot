{
  "name": "tg-gh-action-bot",
  "version": "1.0.0",
  "description": "Telegram Bot to trigger GitHub Actions via Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
